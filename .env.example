# ===========================================
# Hytale Panel Configuration
# ===========================================
# Copy this file to .env and modify values

# --- Panel Authentication ---
# CHANGE THESE! Default credentials are insecure
PANEL_USER=admin
PANEL_PASS=admin

# --- SSO / Reverse Proxy ---
# Set to true to disable panel auth (when using external SSO)
# DISABLE_AUTH=true

# Optional: Set a fixed JWT secret (auto-generated if not set)
# JWT_SECRET=your-random-secret-here

# --- Panel Settings ---
PANEL_PORT=3000

# Timezone for container logs (e.g., America/Chicago, Europe/London)
# TZ=UTC

# URL path prefix for reverse proxy (e.g., /panel for domain.com/panel/)
# BASE_PATH=/panel

# --- Data Storage ---
# IMPORTANT: Must be an absolute path for server volumes to work correctly
# Default uses ${PWD}/data (Linux/Mac only)
#
# WINDOWS: You MUST set this manually! ${PWD} doesn't work on Windows.
# Use forward slashes (/) not backslashes (\)
# HOST_DATA_PATH=C:/Users/YourName/hytale/data
#
# Linux/Mac example:
# HOST_DATA_PATH=/home/user/hytale/data

# --- Modtale Integration ---
# Get your API key from https://modtale.net
# MODTALE_API_KEY=your-modtale-api-key

# --- CurseForge Integration ---
# Get your API key from https://console.curseforge.com
# NOTE: CurseForge keys contain $ characters - wrap in single quotes!
# CURSEFORGE_API_KEY='$2a$10$your-key-here'
